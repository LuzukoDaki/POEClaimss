@{
    var userRole = Context.Session.GetString("UserRole");
}

<div style="display:flex; width:90%; height:50px; color:white; background-color:black; gap:10px; align-items:center; padding:5px;">
    <a href="@Url.Action("Index", "Home")">
        <button class="gray-btn">Home</button>
    </a>
